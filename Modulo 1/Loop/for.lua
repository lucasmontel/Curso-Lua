for i = 1, 10, 1 do
    local nome = "Lucas"
    print("Meu nome é: " .. nome)
end
--[[ A ordem é a mesma, só muda a sintaxe
    for(let i = 0; i < limite; i++){
        code
    }
    Exemplo:
    for i(inicio), limite, incremento(lembre de i++) do(faça)
        code
    end
]]